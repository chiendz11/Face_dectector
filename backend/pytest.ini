[pytest]
addopts = -p no:cacheprovider -m "not integration"
norecursedirs = .tmp .local-pydeps .pytest_cache pytest-cache-files-*
markers =
	integration: tests that run against a live compose or deployed environment
